require("focus").setup {
  enable = true,
  split = {
    bufnew = true,
  },
  ui = {
    winhighlight = true, -- Highlight focused and unfocused windows
    signcolumn = false, -- Signcolumn on all windows
  },
}
